# Generated by Chef, local modifications will be overwritten

init_config:
<% @init_config.each do |key, value| %>
  <% if value.is_a?(Array) %>
<%= "  #{key}: " %> [<%= value.map { |val| %Q/"#{val}"/ }.join(', ') %>]
  <% else %>
<%= "  #{key}: #{value}" %>
  <% end %>
<% end %>

instances:
<% @instances.each do |inst| %>
  <% new_item = TRUE %>
  <% inst.each do |key, value| %>
    <% if new_item %>
      <% if value.is_a?(Array) %>
<%= "  - #{key}: "%> [<%= value.map { |val| %Q/"#{val}"/ }.join(', ') %>]
      <% else %>
<%= "  - #{key}: #{value}" %>
      <% end %>
      <% new_item = FALSE %>
    <% else %>
      <% if value.is_a?(Array) %>
<%= "    #{key}: "%> [<%= value.map { |val| %Q/"#{val}"/ }.join(', ') %>]
      <% else %>
<%= "    #{key}: #{value}" %>
      <% end %>
    <% end %>
  <% end %>

<% end %>

